# Backend Setup Guide

1. Requires Java 21+ and Maven.
2. Compile backend:
   ```bash
   ./mvnw.cmd clean compile
   ```
3. Run backend:
   ```bash
   ./mvnw.cmd spring-boot:run
   ```
4. Health API endpoint:
   `http://localhost:8080/api/health`
